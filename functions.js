function tellJoke() {
  console.log("How do you make 'Seven' even?\nYou take away the 'S'")
}

tellJoke();


function NameofFunction() {
  // body of function
}

function addNumbers(num1, num2, num3, num4) {
  num1 + num2 + num3 + num4
}


addNumbers(1, 2, 3, 4);

/*
Call or invoke a function by typing the NameofFunction()
and place any arguments inside the parenthesis.
Some functions don't take in any parameters.

Analogy:
  Parmeters is like the parking spaces
  Arguments is like the automobiles
So arguments go inside the parameters of a functon?

A variable can get the value of a function without a NameofFunction.
This is called an anonymous function
*/
